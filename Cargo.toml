[package]
name = "ai_rp_backend"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.38", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dotenvy = "0.15"
# axum = "0.7"
# tower = "0.4"
# tower-http = { version = "0.5", features = ["cors"] } # Для настройки CORS (чтобы браузер не блокировал запросы)